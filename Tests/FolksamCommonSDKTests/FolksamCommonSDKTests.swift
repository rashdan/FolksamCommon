import XCTest
@testable import FolksamCommonSDK

final class FolksamCommonSDKTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FolksamCommonSDK().text, "Hello, World!")
    }
}
